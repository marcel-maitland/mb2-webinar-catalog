[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so /admin, /admin/import, /admin/events/:id all work
# when refreshed or deep-linked.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
